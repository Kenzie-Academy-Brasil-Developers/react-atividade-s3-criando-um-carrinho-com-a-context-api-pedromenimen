import styled from "styled-components";

export const PopoverContainer = styled.div`
  i {
    font-size: xx-large;
    margin: 15px;
    position: absolute;
    right: 0;
    top: 0;
  }
  i:hover {
    cursor: pointer;
  }
  PopoverContent {
    width: 200px;
  }
`;
